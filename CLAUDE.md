# CLAUDE.md — Smart Contracts (Foundry/Solidity)

## Stack

Solidity 0.8.x · Foundry · OpenZeppelin v5 (standard + upgradeable) · ERC1967 Transparent Proxy · Arbitrum Sepolia

## Commands

```bash
forge build              # compile (optimizer + via_ir enabled)
forge test               # run all tests
forge test -vvvv         # verbose with traces
forge fmt                # format
anvil                    # local chain
./bin/run-all.sh         # deploy all contracts (18-step orchestration)
./bin/export-abi.sh      # export ABIs to abi/
```

## Architecture

```
src/
├── core/
│   ├── balance-ledger/
│   │   ├── BalanceLedger.sol            # 3-state balance model + on-chain collateral flag (upgradeable)
│   │   └── BalanceLedgerStorage.sol     # Storage layout with gap (frozen at 49 slots)
│   ├── centuari/
│   │   ├── Centuari.sol                 # Main lending/borrowing (upgradeable), reads/writes BalanceLedger
│   │   ├── CentuariStorage.sol          # Storage layout with gap
│   │   ├── CentuariBondERC20.sol        # Bond token (ERC20)
│   │   └── CentuariBondERC20Factory.sol # Bond token factory
│   ├── collateral/
│   │   ├── CollateralManager.sol        # Unflag-while-in-debt path with 24h flag-lock + RiskModule gate (upgradeable)
│   │   └── CollateralManagerStorage.sol # Storage layout with gap
│   ├── cross-chain/
│   │   ├── HubDepositor.sol             # Hub-native direct deposit/payout (upgradeable)
│   │   ├── HubDepositorStorage.sol      # Storage layout with gap
│   │   ├── WithdrawalRegistry.sol       # Withdrawal state machine + HF gate (upgradeable)
│   │   ├── WithdrawalRegistryStorage.sol # Storage layout with gap
│   │   ├── HubIntentSettler.sol         # Solver fill + BalanceLedger credit (upgradeable)
│   │   ├── HubIntentSettlerStorage.sol  # Storage layout with gap
│   │   ├── SettlementLedger.sol         # Solver reimbursement tracking (upgradeable)
│   │   └── SettlementLedgerStorage.sol  # Storage layout with gap
│   ├── risk/
│   │   └── RiskModuleStub.sol           # Phase 1 fail-closed stub (canUnflag always false)
│   └── settlement/
│       ├── Settlement.sol               # Batch settlement processor (upgradeable)
│       └── SettlementStorage.sol        # Storage layout with gap
├── interfaces/
│   ├── IBalanceLedger.sol
│   ├── ICentuari.sol
│   ├── ICollateralManager.sol
│   ├── IRiskModule.sol
│   ├── ISettlement.sol
│   └── cross-chain/
│       ├── IHubDepositor.sol
│       ├── IWithdrawalRegistry.sol
│       ├── IHubIntentSettler.sol
│       └── ISettlementLedger.sol
├── libraries/
│   └── DateTime.sol                     # Date formatting for bond token names
├── mocks/
│   ├── MockToken.sol                    # Test ERC20
│   └── Faucet.sol                       # Testnet token distributor
└── utils/
    └── ReentrancyGuardUpgradeable.sol   # ERC7201 namespaced reentrancy guard

script/                  # Foundry deployment scripts (Deploy*.s.sol, Upgrade*.s.sol, Configure*.s.sol)
test/                    # Foundry tests (*.t.sol)
bin/                     # Shell scripts for deployment orchestration
abi/                     # Exported ABIs (generated by export-abi.sh)
```

### Contract Relationships

```
BalanceLedger (Upgradeable, ERC1967 proxy)
  ↑ balance accounting (available, inOrders, inYieldRouter) + on-chain collateral flag
  ↑ writer-gated: only authorized contracts can mutate
  │
  ├── HubDepositor (Upgradeable) — hub-native deposit/payout, token custody
  ├── Centuari (Upgradeable) — lending/borrowing, auto-flag at settlement, auto-unflag on repay
  ├── Settlement (Upgradeable) — batch settlement, calls Centuari.settleMatch()
  ├── CollateralManager (Upgradeable) — mid-life unflag path, 24h flag-lock + RiskModule gate
  │     └── RiskModuleStub — Phase 1 fail-closed (canUnflag always false)
  ├── WithdrawalRegistry (Upgradeable) — withdrawal state machine + HF gate, debits on request
  │     ├── calls IRiskModule.canWithdraw() as first action
  │     └── calls HubDepositor.payoutDirect() for hub-native withdrawals
  └── HubIntentSettler (Upgradeable) — solver fill for cross-chain deposits, credits user
        └── SettlementLedger (Upgradeable) — solver reimbursement tracking, calls releaseToSolver()
```

### Market Identification

Markets are identified by `bytes32 marketId = keccak256(abi.encode(loanToken, maturity))`. Same loan token with different maturities = different markets.

## Code Standards

### Naming

| Element | Convention | Example |
|---------|-----------|---------|
| Contracts | PascalCase | `Centuari`, `Settlement`, `BalanceLedger` |
| Interfaces | `I` prefix + PascalCase | `ICentuari`, `ISettlement`, `IBalanceLedger` |
| Storage contracts | `*Storage` suffix | `CentuariStorage`, `BalanceLedgerStorage` |
| Functions (external) | camelCase | `settleMatch`, `repay`, `markCollateral` |
| Functions (internal) | `_` prefix + camelCase | `_processMatch`, `_getMarketId` |
| Events | PascalCase | `MarketCreated`, `LendPositionCreated`, `CollateralFlagSet` |
| Errors | PascalCase | `AlreadySettled`, `ContractPaused`, `FlagLockActive` |
| Constants | SCREAMING_SNAKE_CASE | `RATE_PRECISION`, `SECONDS_PER_YEAR`, `MAX_FLAG_LOCK` |
| Storage variables | `_` prefix + camelCase | `_settlement`, `_balanceLedger`, `_marketTotalCbt` |
| Modifiers | camelCase | `onlySettlement`, `onlyOperator`, `whenNotPaused`, `onlyAuthorizedWriter` |
| Structs | PascalCase | `MatchData`, `MarketInfo` |
| Mappings | `_` prefix + descriptive | `_settledMatches`, `_lendPositionCbtAmount` |
| Deploy scripts | `Deploy{Contract}.s.sol` | `DeployCentuari.s.sol`, `DeployBalanceLedger.s.sol` |
| Test files | `{Contract}.t.sol` | `Centuari.t.sol`, `BalanceLedger.t.sol` |

### Clean Code Rules

1. **Separate storage from logic** — upgradeable contracts use `*Storage.sol` abstract contracts. All state variables live in storage contracts with `uint256[N] private __gap` for future upgrades.
2. **Interface-first design** — define all external functions in `interfaces/`. Contracts implement interfaces. Other contracts interact through interfaces, never concrete types.
3. **Access control via modifiers** — use `onlySettlement`, `onlyOperator`, `onlyCentuari`, `onlyAuthorizedWriter`, `whenNotPaused`. Never inline access checks in function bodies.
4. **Custom errors over require strings** — use `error AlreadySettled()` over `require(!settled, "already settled")`. Custom errors are cheaper and more precise.
5. **Events for all state changes** — emit an event for every externally visible state mutation. Off-chain indexers depend on these.
6. **SafeERC20 for all transfers** — always use `SafeERC20.safeTransfer` / `safeTransferFrom`. Never call `transfer` directly.
7. **`initialize()` over constructor** — upgradeable contracts use `initializer` modifier. Use `_disableInitializers()` in constructor to prevent implementation initialization.
8. **ERC7201 for reentrancy guard** — the custom `ReentrancyGuardUpgradeable` uses namespaced storage to avoid storage collisions with proxy patterns.
9. **Rates in basis points** — all rates use `RATE_PRECISION = 10000`. 500 = 5%. Never use raw percentages or floating point.
10. **No magic numbers** — define constants for all numeric values. `365 days` not `31536000`.

### Upgrade Safety

- Never reorder or remove storage variables in `*Storage.sol` — only append
- Always maintain the `__gap` array and reduce its size when adding new variables
- Test upgrades with `UpgradeSettlement.s.sol` pattern — deploy new implementation, call `upgradeAndCall`
- ProxyAdmin controls upgrades — separate from contract owner
- Storage layout snapshots committed in `test/snapshots/` — verify after any storage change

### Deployment Rules

- `run-all.sh` orchestrates the full deployment in 18 steps — maintain this order
- Each script is idempotent where possible — re-running should not break state
- Deployment output goes to `deployments/deploy-<network>-latest.json`
- After deployment, run `export-abi.sh` to update ABI files for off-chain services
- BalanceLedger writer registration uses a two-phase approach: `ConfigureBalanceLedger.s.sol` adds Centuari + HubDepositor first, then Settlement after it's deployed

### Testing Rules

- Use `forge-std/Test.sol` with `vm` cheatcodes
- Test files in `test/` with `.t.sol` suffix
- Use mock contracts for external dependencies (e.g., `MockBalanceLedger`)
- Test all access control paths — verify `onlySettlement`, `onlyOperator`, `onlyAuthorizedWriter` revert correctly
- Test upgrade paths — verify storage layout compatibility
- Test edge cases: zero amounts, expired maturities, duplicate settlements
- Test collateral flag semantics: idempotent mark (no refresh of `_flaggedAt`), flag-lock enforcement, auto-unflag on repay

### Configuration

```toml
# foundry.toml
via_ir = true       # IR-based code generation (required for complex contracts)
optimizer = true    # Optimizer enabled for gas efficiency
```

Remappings: `@openzeppelin/contracts/` → `lib/openzeppelin-contracts/contracts/`, `@openzeppelin/contracts-upgradeable/` → `lib/openzeppelin-contracts-upgradeable/contracts/`
